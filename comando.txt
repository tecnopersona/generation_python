python -V
pip list
pip install django

-- crear entorno virtual
python -m venv env

-- activar le entorno
mientorno\Scripts\activate --win (cmd)


source entornovirtual/Scripts/activate  (bash)


source env/bin/activate --mac/linux

Instalar dependencias


-- crear proyecto
django-admin startproject portafolio


-- accedemos al proyecto
cd nombre_proyecto

-- *instalar las dependencias a partir del archivo requirements.txt 
pip install -r requirements.txt 

-- creamos una aplicación dentro del proyecto
python manage.py startapp login